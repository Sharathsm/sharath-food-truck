# Sharath Food Truck

Simple Flask demo app to demonstrate full CI/CD (Docker -> Jenkins -> Kubernetes).

## Run locally
1. `python3 -m venv venv && source venv/bin/activate`
2. `pip install -r requirements.txt`
3. `python app.py`  # runs on http://0.0.0.0:5000

## Docker
Build:
